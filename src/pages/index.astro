---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import heroImage from '../assets/images/hero.jpg';
---

<BaseLayout
  title="Francesco Giacomi | Photography"
  description="Landscape and travel photography by Francesco Giacomi."
  transparentHeader={true}
>
  <section class="hero">
    <div class="hero__image">
      <Image
        src={heroImage}
        alt="Landscape photography by Francesco Giacomi"
        widths={[800, 1200, 1800, 2400]}
        sizes="100vw"
        loading="eager"
      />
    </div>
  </section>

  <section class="intro" id="intro">
    <div class="container">
      <p>Photographs born from summer travels. Stories told through landscapes, light, and the quiet moments in between.</p>
      <a href="/collections" class="intro__link">View Collections &rarr;</a>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    position: relative;
    width: 100%;
    height: 100vh;
    min-height: 600px;
    overflow: hidden;
  }

  .hero__image {
    position: absolute;
    inset: 0;
  }

  .hero__image :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .intro {
    padding: var(--spacing-2xl) 0;
    text-align: center;
  }

  .intro p {
    font-family: var(--font-sans);
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    line-height: 1.6;
    color: var(--color-text-muted);
    max-width: 700px;
    margin: 0 auto;
  }

  .intro__link {
    display: inline-block;
    margin-top: var(--spacing-lg);
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    border-bottom: 1px solid var(--color-text);
    padding-bottom: 2px;
    transition: opacity var(--transition);
  }

  .intro__link:hover {
    opacity: 0.6;
  }
</style>
